# direnv integration (optional convenience). Loads devenv's direnvrc, then
# activates this project's environment on `cd`. `devenv up` / `devenv shell`
# work without direnv too — this just auto-enters the shell.
eval "$(devenv direnvrc)"

use devenv
