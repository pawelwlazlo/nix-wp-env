<?php
/**
 * Placeholder docroot for NER-208.
 *
 * Renders phpinfo() so a browser hit on http://localhost:8080 confirms the full
 * Caddy → PHP-FPM path works before Bedrock exists. Replaced by the Bedrock
 * application layout in NER-209.
 */
phpinfo();
